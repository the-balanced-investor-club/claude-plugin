---
description: Read the macro backdrop — rates, jobs, growth, commodities
argument-hint: ""
---

Load the `macro-read` skill and show the user what actually moved in the macro data.

Check the `As of` dates before comparing any two indicators — they arrive on different schedules. Never report the raw change in the CPI index as an inflation rate; it is an index, not a rate.
